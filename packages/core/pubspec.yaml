name: core
description: "OMNIGO Core - shared models, repositories, usecases"
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.3
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  equatable: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  freezed: ^3.0.0
  json_serializable: ^6.9.0
  build_runner: ^2.4.0

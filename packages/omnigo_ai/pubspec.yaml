name: omnigo_ai
description: "OMNIGO AI - on-device + API client"
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.11.3
  flutter: ">=1.17.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
